{% extends 'default/index.html.twig' %}

{% block content_header %}
    <h1>Tontine</h1>
{% endblock %}

{% block content %}
    <div class="box box-primary">
        <div class="box-header with-border">
            <h3 class="box-title">Détails de la tontine</h3>
        </div>
        <div class="box-body">
            {% include 'tontineBundle:Tontine:partials/item-details.html.twig' %}
        </div>
        <div class="box-footer">
            <a href="{{ path('tontine_tontines_index') }}" class="btn btn-default" style="margin: 5px;">Retour</a>
            <div class="pull-right">       
                {# <a href="{{ path('tontine_tontines_edit', { 'id': tontine.id }) }}" class="btn btn-success">Edit</a> #}         
                {{ form_start(delete_form, {attr: {style: "display: inline"}}) }}
                    <input type="submit" value="Supprimer" class="btn btn-danger" style="margin: 5px;">
                {{ form_end(delete_form) }}                
            </div>            
        </div>
    </div>            
{% endblock %}
